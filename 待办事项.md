# 待办事项

## 当前结论

- [x] 完成模型驱动的 Tool Loop：处理 `tool.call`，调用 Tool Runtime，并将结果回传给模型继续执行。
- [x] 补齐真实 Tool 执行器：文件读写、文件列表、Git 查询、文本搜索、补丁、注册命令和验证命令；当前 `ToolGateway` 未注入 handler 时只返回 `null`。
- [x] 将 `DurableToolRuntime` 接入 API、Explorer 和 Executor 执行链，而不是仅作为领域层骨架存在。
- [x] 修复当前未提交的 `tool-runtime` 测试失败及 domain typecheck 类型错误。

## MCP 接入

- [x] 增加 MCP Client/Server 适配层和依赖。
- [x] 增加 MCP Server 配置：名称、启动命令、参数、环境变量、传输方式和超时。
- [x] 实现 MCP 能力发现、工具 schema 注册、调用、取消、超时和错误映射。
- [x] 将 MCP 工具纳入 Explorer/Executor 的角色权限、审批、工作区边界和审计策略。
- [x] 持久化 MCP 调用状态，处理幂等、未知结果和重启后的 reconciliation。

## Codex 插件与 Computer Use

- [x] 增加 Codex Plugin 的发现、注册、版本兼容和生命周期管理。
- [x] 建立插件能力到 Factory Tool 的统一 Bridge，包含参数校验、权限校验和结果回传。
- [x] 明确 `computer-use` 的执行边界：由宿主应用、MCP Server 或独立适配器提供，不能假设 `codex app-server` 自动拥有 Codex Desktop 插件能力。
- [x] 为 Computer Use 增加用户确认、敏感操作审批、截图/动作事件、取消、超时和失败恢复。
- [x] 禁止 Explorer 未经授权执行桌面操作；Executor 的 Computer Use 权限必须显式配置。

## API、事件与界面

- [x] 增加 Tool/MCP/Plugin 的状态事件：requested、running、completed、denied、failed、needs-reconciliation。
- [x] 通过 API/SSE 暴露调用进度、审批请求、调用结果和恢复状态。
- [x] 在 ExplorerThread、Run Detail 和审计视图中展示工具调用链及失败原因。
- [x] 修复 Status 中 5 小时限额和 7 天限额的数据链路：通过 `account/rateLimits/read` 读取精确窗口，并在无精确遥测时显示不可用，不使用本地估算。

## 验收

- [x] 增加 Tool Loop、MCP、插件 Bridge 和 Computer Use 的单元测试、协议测试和集成测试。
- [x] 验证 Explorer 只读策略、Executor 受控写入、路径边界、命令白名单和敏感操作审批。
- [x] 验证 App Server 重启、调用超时、进程退出、重复 callId 和未知副作用结果的恢复行为。
- [ ] 完成 domain/API/web 的 typecheck、测试和端到端验证后，再将上述能力标记为已支持。

> 当前自动化 typecheck、测试和 build 已通过；本轮浏览器实测能看到 Status 的 5 小时/7 天限额字段，但本地运行中的 API 端口对浏览器不可达，因此只验证了无遥测时的降级展示。接入能返回精确 `300` 分钟和 `10080` 分钟窗口的 Codex App Server 后，再完成最后一项端到端验收。
